(jbuild_version 1)

(executable
 ((name main_native)
  (public_name rss_to_mail)
  (package rss_to_mail)
  (libraries
   (std lwt cohttp-lwt-unix swaks rss_to_mail scraper cmdliner persistent_data))
  (flags (:standard -open Std))
  (preprocess (pps (lwt_ppx)))))
