(jbuild_version 1)

(executable
 ((name main_native)
  (public_name rss_to_mail.main_native)
  (libraries
   (std lwt cohttp-lwt-unix swaks rss_to_mail containers.sexp))
  (flags (:standard -open Std))
  (preprocess (pps (lwt.ppx)))))
